document.querySelectorAll('.btn-interval').forEach(btn => {
  if (btn.dataset.start == <%= params[:start] %>) {
    btn.classList.add('active');
  } else {
    btn.classList.remove('active');
  }
});
document.querySelector('#interval-last-update').textContent = '<%= last_occupancy_update(@occupancy.created_at) %>';
