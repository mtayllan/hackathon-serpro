<%= form_with(model: dashboard_organization_user, local: true) do |form| %>
  <% if dashboard_organization_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(dashboard_organization_user.errors.count, "error") %> prohibited this dashboard_organization_user from being saved:</h2>

      <ul>
        <% dashboard_organization_user.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
