<h1 class="h3 mb-2 text-gray-800">Usuários</h1>
<p class="mb-4">Aqui você pode visualizar e alterar os usuários da sua organização.</p>
<div class="card shadow mb-4">
  <div class="card-header py-3">
    <div class="row align-items-center">
      <div class="col">
        <h5 class="m-0 font-weight-bold text-primary">Listagem de Usuários da Organização</h5>
      </div>
      <div class="col text-right">
        <%= link_to 'Novo usuário', new_dashboard_organization_user_path, class: 'btn btn-sm btn-primary' %>
      </div>
    </div>
  </div>
  <div class="card-body">
    <div class="table-responsive">
      <table id="organization-user-datatable" class="table" data-source="<%= dashboard_organization_users_path(user: current_organization_user, format: :json) %>">
        <thead>
          <tr>
            <th>ID</th>
            <th>Email</th>
            <th>Ações</th>
          </tr>
        </thead>
        <tbody>
        </tbody>
      </table>
    </div>
  </div>
</div>

<script>
  $(() => {
    $('#organization-user-datatable').dataTable({
      'processing': true,
      'serverSide': true,
      'ajax': {
        'url': $('#organization-user-datatable').data('source')
      },
      'pagingType': 'full_numbers',
      'columns': [
        { 'data': 'id' },
        { 'data': 'email' },
        { 'data': 'actions' }
      ]
    });
  });
</script>
